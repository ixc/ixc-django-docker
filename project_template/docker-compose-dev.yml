version: '3.6'

x-project: &project
  volumes:
    - project_template-sync:/opt/project_template:nocopy

services:
  celery: *project
  celerybeat: *project
  celeryflower: *project
  gunicorn: *project
  shell: *project

volumes:
  project_template-sync:
    external: true
