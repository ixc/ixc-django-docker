# This additional context is provided to Django and Jinja2 templates via
# `ixc_django_docker.context_processors.environment` and
# `ixc_django_docker.jinja2.environment`.
def environment(request):
    return {}
